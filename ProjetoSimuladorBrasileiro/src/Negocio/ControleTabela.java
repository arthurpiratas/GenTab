package Negocio;

import Basicas.Jogadores;
import Basicas.Tabela;
import Basicas.Clube;
import Repositorio.RepositorioClubeArray;
import Repositorio.RepositorioPartidaArray;;


public class ControleTabela extends Tabela {
	
	/*RepositorioPartidasArray[380] Tabela = new RepositorioPartidasArray();
	
	void gerarPartidasdeumClube(Clube clube) {
		for(int a=0; a<qtdClubes; a++) {
			if(clube equals(RepositorioClubeArray repositorio[a])) {
				Clube auxiliar = repositorio[a];
				for(int b=0; b!=a && b<20;b++) {
					int rodada= 1;
					Tabela[b] = Tabela.inserirPartidas(Partida(clube, repositorio[b], data, rodada, statusPartida));
					rodada++;
				}
			}
			break;
		}
	}
	
	void gerarTabela() {
		for(int a=0; a<qtdPartidas; a++) {
			if(tabela[a].getPartidas()!=null) {
				System.out.println("Partida: "+ tabela[a].getClubeCasa() +" x " + tabela[a].getClubeFora() );
			}
		}
	};
	void zerarTabela() {
		for(int a=0; a<index; a++) {
			if(tabela[a].getPartidas()!=null) {
				tabela[a].setPartidas(null);
			}
		}*/
	}
